 # AI 财报智审助手 — 设计规约 v0.2

 > 最后更新: 2026-07-25
 > 版本: v0.2-draft
 > 状态: 修订中 — 基于 v0.1 评审反馈补充 error path、中文表格兜底、对话持久化与量化的验收方法

 ---

 ## 1. 用户与场景

 ### 1.1 目标用户画像

 | 角色 | 典型场景 | 核心诉求 | 优先级 |
 |---|---|---|---|
 | **个人投资者** | 拿到一份上市公司 PDF 财报，没时间逐页细看，想直接问 AI 关键指标、异常波动原因 | "快速理解这份财报的核心信息" | P0 |
 | **金融/会计专业学生** | 做财报分析作业，用工具辅助理解数据趋势、制作分析图表 | "帮我拆解数字背后的逻辑" | P1 |
 | **初级分析师** | 快速核对多份财报的关键数据，验证自己的判断是否有盲区 | "帮我查一下这个数据在第几页" | P2 |

 ### 1.2 用户故事

 **US-01 (P0)** 作为个人投资者，上传一份财报 PDF 后，我希望 AI 能直接回答"营收多少""净利润多少""毛利率多少"这类事实性问题，不需要我自己翻页找。

 **US-02 (P0)** 作为个人投资者，我希望能追问"为什么 Q3 营收增长但现金流下降"，AI 能给出基于财报数据的合理解释并注明数据来源页码。

 **US-03 (P0)** 作为个人投资者，当我要求"把毛利润变化做成柱状图"时，AI 能生成图表直接嵌入对话，而不是给我一段文字描述。

 **US-04 (P1)** 作为会计专业学生，我上传了茅台和五粮液的两份年报后，希望问"谁的负债率更低"，AI 能跨文档对比回答。

 **US-05 (P0)** 作为任何用户，在聊天页点"返回首页"时，已上传的财报列表和部分对话仍在；刷新页面或关闭标签页则全部清除。*（补充：关闭前未下载报告时，系统应提示用户对话将丢失）*

 **US-06 (P0)** 作为任何用户，我希望在完成分析后能一键下载一份排版整洁的 PDF 报告，包含我的所有问题和 AI 答复，且对话中的交互式图表能正确转为静态图像嵌入报告中。

 **US-07 (P0)** 作为非技术背景的个人投资者，我第一次使用时希望能有清晰的引导，告诉我如何获取 API Key 以及在哪里填入，如果填错了能立刻知道。

 **US-08 (P0)** 作为深度用户，如果我的 API Key 失效或额度耗尽，我希望系统明确告知我是 Key 的问题，而不是含糊地说"AI 暂时无法回答"。

 **US-09 (P1)** 作为普通用户，如果一次会话中我提问了较多问题，我希望系统能自动管理历史记录，不要让上下文变得过慢或丢三落四。

 **US-10 (P1)** 作为谨慎的用户，如果浏览器意外崩溃或手滑关闭标签页，我希望至少能找回这段会话（通过手动保存或自动备份机制）。

 ---

 ## 2. 功能范围

 ### 2.1 P0 — 核心体验（完不成不发布）

 | 功能 | 说明 | 验收条件 |
 |---|---|---|
 | PDF 上传 | 拖拽或点击选择 PDF 文件，支持最大 50MB | 上传后进入解析中状态，完成后自动进入聊天页 |
 | PDF 解析 | 后台用 Unstructured 提取文本块和表格；若表格提取质量低于阈值（如合并单元格错位、关键行列缺失 >= 20%），自动降级为纯文本提取+标注"表格提取不完整，建议对照原文"；提供解析进度条反馈 | 100 页年报解析完成 <= 25s；解析失败时有明确错误提示和重试选项 |
 | 向量化存储 | 解析结果分片，embedding，Chroma；遇到 Chroma 磁盘写入失败时，回退到内存中的临时检索（提示用户"本地持久化缓存异常，本次会话依然可用，但刷新后需重新解析"） | Chroma collection 以文件 hash 为标识持久化到本地 |
 | BYOK 设置 | 用户填入自己的 API Key，选择提供商和模型；设置页提供一键跳转链接（如 `https://platform.openai.com/api-keys`）、格式校验和连通性测试按钮 | Key 存 localStorage（跨会话保留），绝不经过服务端 |
 | 聊天问答 | 基于财报上下文的 AI 对话，支持追问；API 调用失败时，按错误类型给予差异化提示：401→"Key 无效"，429→"额度已用尽，请稍后再试"，其余→"LLM 服务异常，请检查设置"；LLM 响应超时时提问方保留输入，可重试 | 事实性问题准确率 >= 95%（详见 5.1 节评测方案） |
 | 图表生成 | 用户要求图表时，AI 输出结构化数据，Plotly 渲染；系统对生成的图表数据做值域校验（数据点是否存在、与检索片段是否矛盾），校验未通过时附加"以下图表数据可能存在偏差，建议对照原文后再使用"提示 | 图表生成成功率 >= 90% |
 | 同标签页导航 | 首页和聊天页切换不丢失会话 | sessionStorage 方案，关标签页或刷新即清除；关闭前如果存在活跃会话且未导出报告，弹出确认提示 |
 | 报告下载 | 将聊天记录渲染为排版好的 PDF；对话中的 Plotly 图表转为 PNG 静态图像嵌入；如对话过长（超过 50 轮问答），自动截断为最近 50 轮并在报告开头标注 | 含所有问答与图表，格式可读可打印 |
 | 无 Key 渐进体验 | 未设置 Key 前可上传并解析 PDF、完成向量化；解析完成后引导用户设置 Key；Key 设置完成后自动跳回聊天界面并保留解析结果；即使用户离开页面再回来设置 Key，已解析的文件索引保留在 localStorage 中，不会被 sessionStorage 清空影响 | 上传解析不受阻，聊天输入框显示引导文案直接提供注册链接；设置 Key 后恢复可用 |
 | API Key 管理 | 设置页可填入、修改、清除、查看 Key；填入实时做格式校验（API Key 前缀/长度规则）；内置"测试连接"按钮发送一条空请求验证 Key 有效性；清除后 localStorage 中 Key 被移除，页面回到未设置状态 | 清除后 localStorage 中 Key 被移除，页面回到未设置状态 |
 | 数据清除 | 一键清除 Chroma 向量缓存 + localStorage + sessionStorage；若 Chroma 目录不存在或已损坏，清除操作仍应正常清理其余部分并报一条可忽略的日志 | 二次确认后所有数据回到初始状态 |
 | 会话持久化保护 | 用户在首页可以将当前会话"锁定"（导出到 localStorage 中存储为按时间戳命名的快照，限制最多保留 3 个快照）；浏览器刷新或意外关闭后，可通过首页"恢复会话"入口选择历史快照恢复对话和当前活跃文件索引 | 锁定的会话在浏览器重启后可被用户主动恢复 |

 ### 2.2 P1 — 增强体验

 | 功能 | 说明 |
 |---|---|
 | 多份财报交叉对比 | 同时加载多个 collection，AI 跨文档检索回答 |
 | 预设 Prompt 模板 | "总结三大风险"、"提取所有数据表" 等一键快捷提问 |
 | 图表嵌入 PDF | 下载报告时 Plotly 图表转 PNG 嵌入 PDF（P0 已实现） |
 | 文字选中高亮+追问 | 对 AI 回答中某一句选中后追加提问 |
 | 对话管理与搜索 | 长对话支持翻页浏览、关键词搜索历史问答；系统自动管理上下文窗口，超出限制时智能摘要旧轮次后丢弃 |
 | 会话自动备份 | 每 10 轮问答或每 10 分钟自动保存一次会话快照到 localStorage（最多保留最近 5 个自动快照），浏览器崩溃后可恢复最近的快照 |
 | 图表数据手动校正 | 用户可对 AI 生成的图表数据点进行手动修正（修改数值或排除异常数据点），修正后自动更新图表 |

 ### 2.3 P2 — 远期展望

 - 财报日历提醒
 - 关键词搜索多份历史报告
 - 数据导出为 xlsx
 - 英文财报支持
 - 图片型 PDF（扫描件 OCR）
 - 智能摘要回收：LLM 自动压缩历史对话为结构化摘要，释放 context 窗口

 ### 2.4 显式不做的功能

 | 不做 | 原因 |
 |---|---|
 | 不提供投资建议或买卖判断 | 法律风险，AI 限定在事实分析层面 |
 | 不建登录或注册系统 | 隐私策略=零服务端存储，登录与此矛盾 |
 | 不存原始 PDF 到服务器 | 解析完成即可释放，节省存储+规避版权 |
 | 不做实时行情或看盘功能 | 专注已发布财报文本分析，不是交易终端 |
 | 不做手机原生 App | MVP 阶段 Web 适配移动端即可 |
 | 不支持扫描件 OCR | Unstructured 对纯图片 PDF 效果差，需额外 OCR 管线；P2 考察 |
 | 不承诺跨浏览器数据恢复 | 会话保护机制依赖浏览器本身存储能力，不同浏览器清除策略不同，用户应了解此限制 |

 ---

 ## 3. 约束规矩

 ### 3.1 架构约束

 1. **前后端统一 Python 栈**: FastAPI + Jinja2，不引入 Node.js/React。例外：将来若前端复杂度超出 Jinja2 能力边界，经技术评审后可引入轻量级前端框架。

 2. **BYOK 原则（Bring Your Own Key）**: 绝不内嵌任何形式的 API Key、预付费额度或代理转发。用户必须提供自己的 API Key。Key 的存储仅限于浏览器 localStorage。

 3. **零服务端用户数据**: 服务端不创建数据库、不写用户文件。PDF 解析完成后原始文件立即释放。聊天记录仅在 sessionStorage 中存活。服务重启后一切归零。

 4. **开源 AI 接口层**: 底层 LLM 接口必须抽象为统一协议，BYOK 模式下可随时切换 OpenAI、通义千问、DeepSeek 或 Ollama 本地。考虑到本地模型能力差异较大，使用 Ollama 等本地模型时的回答质量和图表生成成功率不纳入 P0 验收标准，仅在设置页标注"本地模型效果可能低于云端模型"。

 5. **两层浏览器存储**:
    - `localStorage`：存 API Key、提供商选择、文件索引（文件名->collection_id 映射）、手动锁定的会话快照（最多 3 个）以及自动备份的快照（最多 5 个）。跨会话持久化。
    - `sessionStorage`：存聊天消息和当前活跃会话状态，刷新或关标签页即清空。
    - 设计意图：Key 配置不应随刷新丢失；对话历史默认属于隐私信息应当刷新即消；用户可通过"锁定"功能主动将关键会话持久到 localStorage 并选择恢复。

 6. **Chroma 持久化与隔离策略**:
    - 每份财报创建独立 Chroma collection，命名规则 `pdf_{sha256前8位}`，保存在 `./chroma_data/`。
    - 提问时只检索当前活跃 collection，多份财报互不污染。
    - Chroma 数据留在用户本机硬盘，不通过网络传输。
    - 隐私承诺指"服务端不存用户数据"；Chroma 缓存属于用户本机数据管理范畴。
    - **Chroma 异常处理**：若 collection 打开失败或写入异常，系统自动回退到内存检索模式（基于原始分片做逐次 embedding + cosine 检索），性能下降但功能不受阻，并提示用户"Chroma 本地缓存异常，已切换到内存模式"。
    - **Chroma 数据恢复**：若 `./chroma_data/` 目录缺失或损坏，清除所有数据时应打印日志但不中断流程；用户可手动删除目录后重新解析。

 7. **数据清除机制**:
    - 首页和设置页各提供"清除所有数据"按钮。
    - 触发后：(a) 删除 `./chroma_data/` 整个目录，(b) 清空 localStorage，(c) 清空 sessionStorage。
    - 执行前弹出二次确认。
    - 如果在 Chroma 目录不存在的状态下触发清除，仅清理存储层并提示"Chroma 缓存已不存在，已清除其余数据"。

 8. **API 错误处理策略**:
    - LLM 调用错误码映射表：
      | 错误场景 | 用户端文案 |
      |---|---|
      | 401 Unauthorized | "API Key 无效或已过期，请前往设置页检查并更新 Key。" |
      | 429 Rate Limit | "API 调用次数已用尽，请稍后重试，或检查 API 额度。" |
      | 500 / 网络超时 | "LLM 服务暂时无响应，请稍后重试。若持续失败，请检查 API 配置。" |
      | 上下文超长（Token 溢出） | "当前对话过长，AI 无法一次处理。已自动截断部分历史内容，你可继续提问。" |
      | 未知错误 | "AI 回复异常，错误已记录。请重试或联系开发者。" |
    - 所有错误在服务端走统一日志格式 `[LLM_ERROR] provider={provider} model={model} code={status_code} msg={truncated_message}`。

 9. **CORS 说明**：当前版本前后端同源（FastAPI + Jinja2），无需 CORS 配置。仅在未来前后端分离时启用 CORS，届时按最小原则配置白名单。

 ### 3.2 回答口径约束

 AI 回复必须遵循引证式分析口径:

 - 所有财务数据必须引用来源页码
 - 不得给出买入/卖出/持有建议
 - 不得预测未来股价或业绩
 - 对于找不到的数据，明确说"财报中未找到该数据"，不编造

 ### 3.3 技术栈约束

 | 层 | 技术选型 | 原则 |
 |---|---|---|
 | 后端框架 | FastAPI 3.x | Python 生态，异步原生，文档自动生成 |
 | 前端模板 | Jinja2 | 统一 Python 栈，部署简单 |
 | PDF 解析 | Unstructured partition_pdf | 开源、支持表格提取、中文支持好；表格提取失败时降级到 PyMuPDF 纯文本提取 |
 | 向量数据库 | Chroma | 嵌入式，无需独立服务，数据在本地；异常时回退到内存检索 |
 | Embedding | text-embedding-3-small (OpenAI) 或 text-embedding-v2 (通义) | 随 LLM 提供商切换 |
 | 图表 | Plotly | 交互式 HTML 图表，直接嵌入网页；报告导出时转为 PNG |
 | PDF 导出 | WeasyPrint | CSS 驱动排版，HTML 转 PDF 保真度好 |
 | 前端样式 | 纯 CSS（无框架） | 保持轻量，减少依赖 |
 | 表格提取兜底 | PyMuPDF（fitz） | Unstructured 表格提取不良时，退回到 PyMuPDF 按坐标提取纯文本并标记"表格提取不完整" |

 ---

 ## 4. 关键决策

 ### 4.1 技术选型决策

 | 决策点 | 选项 | 选定 | 理由 |
 |---|---|---|---|
 | 前端方案 | Streamlit / Jinja2 / React | Jinja2 | 平衡开发效率和 UI 自由度，统一 Python 栈 |
 | LLM 接口设计 | 硬编码 / 抽象层 / 插件化 | 抽象层 | 支持多提供商切换，为 BYOK 提供技术基础 |
 | 向量数据库 | Chroma / FAISS / Pinecone | Chroma | 嵌入式、免运维、Python 原生；异常时回退到内存检索 |
 | PDF 解析器 | Unstructured / pdfminer / PyMuPDF | Unstructured + PyMuPDF 兜底 | Unstructured 主用表格提取；检测到提取质量过低时自动降级到 PyMuPDF 纯文本提取并提示用户 |
 | 图表方案 | Matplotlib / Plotly / ECharts | Plotly | 交互式 HTML，可直接嵌入聊天 |
 | PDF 导出 | WeasyPrint / ReportLab / wkhtmltopdf | WeasyPrint | CSS 驱动排版，HTML 转 PDF 保真度好 |
 | 会话存储 | 服务端 DB / localStorage / sessionStorage / 混合 | 混合: sessionStorage 默认 + localStorage 主动锁定 | 默认隐私优先，用户按需持久化 |
 | 持久化索引 | 数据库 / localStorage / 无 | localStorage | 记录文件->collection 映射，不存内容 |
 | 表格提取质量检测 | 自动检测每张表格有效行数 vs 预期行数 | 提取到 < 3 行或合并单元格比例 > 30% 时自动标记异常 | 减少用户因错表做错误分析的风险 |
 | 问答上下文管理 | 固定窗口 / 智能摘要 / 全部保留 | 固定窗口（最近 3000 token）+ 超过后自动摘要历史 | 兼顾长对话可用性和幻觉控制 |

 ### 4.2 产品决策

 | 决策 | 选定 | 原因 |
 |---|---|---|
 | 用户是否需要注册 | 不需要 | 零存储策略，无注册基础 |
 | PDF 由谁提供 | 用户自行提供 | 规避版权风险，财报均为公开披露 |
 | 是否支持多份财报同时上传 | P1 阶段支持 | P0 先做单份 |
 | 默认的 API 提供商 | 用户在设置页选择 | BYOK 原则 |
 | 是否缓存 embedding | Chroma 本地持久化，按 hash 隔离 | 同一份财报再次打开不用重新向量化；换财报不互相污染 |
 | API Key 清除方式 | 设置页"清除 Key"按钮或"清除所有数据"按钮 | 适配借用他人电脑或切换机房场景 |
 | 无 Key 用户如何上手 | 设置页内置注册引导链接 + Key 格式说明与测试连接 | 降低非技术用户配置门槛 |
 | 浏览器崩溃后能否找回会话 | 仅限用户主动锁定或自动备份的快照（localStorage） | 隐私与便利的平衡选择 |
 | 本地模型质量预期管理 | 设置页标注"本地模型效果可能低于云端模型"，P0 验收不涵盖本地模型 | 避免本地模型拖慢整体发布节奏 |

 ### 4.3 风格决策

 | 维度 | 选型 | 原因 |
 |---|---|---|
 | 视觉风格 | 工具感、信息密集、素净 | 面向分析场景，非营销场景 |
 | AI 回答风格 | 引证式、中性、数据驱动 | 规避建议类内容，提升可信度 |
 | 颜色方案 | 浅色为主，暗色为可选项 | 适合长时间阅读财报和分析 |
 | 交互模式 | 键盘优先（Enter 发送）、快捷键 | 提高重复操作效率 |

 ---

 ## 5. 验收标准

 ### 5.1 功能验收

 *评测方法与准确率验证方案*：
 - 构建 10 份公开 A 股年报的评测集，每份包含 10 个事实性 QA（共 100 题），题目覆盖营收、净利润、毛利率、负债率、现金流 5 个维度。
 - 准确率 = 正确回答数 / 有效回答总数（"财报中未找到该数据"算正确回答，编造数据算错误）。
 - 每份年报的 QA 由人工标注答案和页码。
 - 评测在 P0 开发完成后、beta 阶段跑通一次，后续版本变更时增量回归。

 | 测试场景 | 验收条件 | 测试方式 |
 |---|---|---|
 | 上传一份 50 页财报 PDF | 解析完成 <= 20s，自动跳转到聊天页 | 手动 + 计时 |
 | 上传一份 100 页财报 PDF | 解析完成 <= 25s | 手动 + 计时 |
 | 表格提取评估 | 财报中的关键财务数据表能被正确提取出至少 7/10 的关键行数据（如营收、净利润逐行）；否则标记"提取不完整" | 对照原始 PDF 逐行比对 |
 | 表格提取降级 | 面对复杂合并单元格表格（如银行财报），提取质量低于阈值时自动标注"表格提取不完整" | 手动检查标注 |
 | 问"2023 年营收是多少" | AI 回答数字正确，并注明页码 | 对照原始 PDF + 评测集验证 |
 | 追问"为什么营收增但利润降" | AI 引用财报中的费用或成本数据解释 | 手动检查逻辑一致性 |
 | 问"画个毛利率柱状图" | 对话中出现可交互的 Plotly 柱状图；若图表数据与原文矛盾，附加偏差提示 | 视觉确认 + 数据核对 |
 | 点"返回首页"再进聊天页 | 历史对话保留，PDF 无须重新上传 | 手动 |
 | 关闭标签页前有活跃会话 | 弹出"对话将在关闭后丢失，是否已下载报告？"确认提示 | 手动 |
 | 刷新页面 | 聊天记录清除，已解析文件列表清除，API Key 保留 | 手动 |
 | 锁定的会话在浏览器重启后可恢复 | 首页"恢复会话"入口可见，点击后恢复历史对话和活跃文件 | 手动（关闭再打开浏览器） |
 | 下载分析报告 | 下载到一份 PDF，内容含全部问答，图表以 PNG 嵌入 | 打开 PDF 确认 |
 | 修改 API Key 设置 | 后续请求自动使用新 Key | 抓包确认 Header 变化 |
 | 填入无效 API Key | 设置页"测试连接"按钮返回错误，聊天输入框持续显示引导文案 | 手动 |
 | API 返回 429 错误 | 聊天界面显示"API 调用次数已用尽，请稍后重试"提示，不影响已输入但未发出的消息 | 手动 + mock |
 | 清除 API Key | localStorage 中 Key 被移除，聊天页回到引导状态 | 手动 |
 | 清除所有数据 | Chroma 目录清空，localStorage/sessionStorage 清空 | 手动确认 |
 | 清除数据时 Chroma 目录已不存在 | 其余数据仍然正常清除，终端日志记录"Chroma 目录不存在"，页面提示"已清除其余数据" | 手动 + 确认日志 |
 | 未设置 Key 时上传 PDF | 可成功上传解析，聊天输入框显示"请先在设置中填入 API Key"引导文案（含注册链接） | 手动 |
 | 未设置 Key 时完成解析，设置 Key 后恢复 | 设置 Key 后自动跳回聊天，已解析财报无需重新上传 | 手动 |
 | 上传第二份财报后提问 | AI 只基于当前活跃财报回答，不混入上一份数据 | 对照验证 |
 | 评测集准确率 | 事实性问题准确率 >= 95%，有效回答率 >= 98% | 运行 100 题评测集 |

 ### 5.2 性能验收

 | 指标 | 标准 | 条件 |
 |---|---|---|
 | PDF 解析时间 | <= 25s / 100 页（含表格检测），<= 20s / 50 页 | 本地运行，Unstructured 默认配置；解析超时 60s 后显示失败 |
 | 首条回答时间 | <= 5s（不含 LLM 推理） | 向量检索 + Prompt 组装 |
 | 图表渲染时间 | <= 2s | Plotly 生成 HTML 片段 |
 | 首页加载时间 | <= 1s | 冷启动首次加载 |
 | 图表数据校验时间 | <= 0.5s（不影响用户感知） | 值域校验 + 与原文片段对比 |
 | 会话锁定写入时间 | <= 0.5s | 写入 localStorage |

 ### 5.3 安全验收

 | 项目 | 标准 |
 |---|---|
 | API Key 传输 | 前端直连 LLM API，不经过本服务端 |
 | 用户数据残留 | 服务端 /tmp 目录无 PDF 遗留文件 |
 | XSS 防护 | AI 回复内容 HTML 转义，Plotly HTML 用 sandbox iframe 隔离 |
 | CSRF 防护 | 采用同源策略；未来前后端分离时按最小原则配置 CORS 白名单 |
 | 数据清除完整性 | "清除所有数据"后 Chroma 目录、localStorage、sessionStorage 全部为空（若 Chroma 目录已不存在，标记为"部分清除：Chroma 已不存在，其余已清除"） |
 | API Key 本地存储风险提示 | 设置页底部显示提示："API Key 存储在浏览器本地，请勿在公共电脑上使用此功能。" |

 ---

 ## 6. 版本与变更

 ### 6.1 版本规划

 | 版本 | 范围 | 目标日期 | 状态 |
 |---|---|---|---|
 | v0.1-alpha | P0 全部功能，本地可跑通一条完整链路 | TBD | 设计中 |
 | v0.1-beta | P0 功能完善 + 用户体验打磨 + 错误处理 + 表格提取降级 + API 错误路径 | TBD | 待规划 |
 | v0.1-rc | 全链路验证 + 性能验收 + 安全验收 + 评测集跑通 | TBD | 待规划 |
 | v0.1 | 正式发布 | TBD | 待规划 |
 | v0.2 | 加入 P1 功能（多财报对比、预设 Prompt、会话管理、备份等） | TBD | 待规划 |
 | v0.3 | P2 功能（OCR、Excel 导出、日历提醒） | TBD | 待规划 |

 ### 6.2 变更记录

 | 日期 | 版本 | 变更内容 | 作者 |
 |---|---|---|---|
 | 2026-07-25 | v0.2-draft | 补充大量 error path：API 错误码映射表、Chroma 异常回退、表格提取降级与质量检测；补充会话锁定与自动备份机制、无 Key 渐进体验的闭环路径优化、BYOK 引导流程（注册链接+格式校验+测试连接）；修正解析速度指标的一致性、增加 CORS 说明与同源策略澄清；增加评测集定义与准确率验证方法；补充本地模型质量预期管理、图表数据校验层、长对话上下文管理策略、关闭前会话丢失提示；统一验收标准、补充无法解析时的错误提示规则；增加安全性说明与公共电脑提示 | Codex |
 | 2026-07-25 | v0.1-draft | 补充两层存储策略、Chroma 持久化与隔离机制、无 Key 渐进体验、数据清除按钮、API Key 清除逻辑 | Codex |
 | 2026-07-24 | v0.1-draft | 初始规约，基于 brainstorming 产出 | Codex |

 ### 6.3 变更流程

 1. 任何功能变更需在本文件顶部增加一行变更记录
 2. P0 范围变更需用户确认
 3. P1/P2 范围变更可标记后通知用户
 4. 架构或技术栈变更需在第 4 节关键决策中更新理由
